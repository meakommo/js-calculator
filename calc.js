var total = ""
var number = 0
var temp = 0
var entry = 0

    function isNumber(entry) {
        console.log("THis is a Number")
    }
    function notaNumber(entry)  {
        console.log("THis is Not a number")
    }
    function equals()  {
        console.log("=")
    } 
    if (entry === !NaN){isNumber(entry)}
    else if (entry === NaN) {notaNumber(entry)}
    else if (entry === "="){equals()}
    else{

    } 